#include "main.h"

/**
  *_isupper - checks for uppercase character
  *@c: charater to check if upper
  *Return: 1 if c is uppercase
  *0 if c is otherwise
  */

int _isupper(int c)
{
	if (c >= "A" && C <= "Z")
		return (1);
	return (0);
}
